kind: Service
apiVersion: v1
metadata:
  name: rookout-agent-service
  labels:
    product: rookout
    app: agent
    type: service
spec:
  selector:
    product: rookout
    app: agent
    type: deployment
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 7486